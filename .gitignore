# Secrets — never commit real keys
.env
.env.*
!.env.example

node_modules
dist

# Convex generated code SHOULD be committed — CI builds typecheck against it
# (docs.convex.dev/cli/reference/codegen)

# Platform build snapshot + generated stubs — not source, don't ship
isolate/
sst-env.d.ts
